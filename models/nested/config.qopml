versions {

  set host Server(Server);
  
  version sc1_1client {
    run host KeysStore(*) {
      run Store1(*)
    }
    run host Server(*) {
      run Server1(sc1_before_db, sc1_after_db)
    }
    run host Client(*){12}[ch1, ch2] {
      run Client1(sc1)
    }
  }
  
}